extends layout

block content
	.row
		for want in wants
			.col-sm-5
				.panel.panel-default
					.panel-thumbnail
						img.img-responsive(src='#{user.gravatar(70)}')
					.panel-body
						p.lead #{want.name}